/**
 * Sākumlapas “virtuālais backup” / versijas atsauce — 1. iterācija (dizains + Home.vue loģika).
 * Maršruta piekļuves notiek caur `router/index.js` (`meta.public` un pirms tam `auth.init()`).
 */
export const HOME_PAGE_SNAPSHOT_VERSION = 1;
